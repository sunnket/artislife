// Superseded by useArtworkStream, which handles the initial seed paint AND live
// filter-driven resolution. Kept as a thin alias so any stale import still works.
export { useArtworkStream as useBootstrap } from './useArtworkStream'
