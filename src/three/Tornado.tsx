// The GPU particle vortex was removed by request — the ARTWORKS themselves now
// form the revolving tornado funnel (see helix.ts + ArtworkPool.tsx). This module
// is intentionally empty.
export {}
