// Mutable global "time-dilation" multipliers, read each frame by ambient motion
// (dust motes, idle drift) and animated by the hero-zoom choreography (Section 6).
// 1 = normal speed, 0.2 = slowed during a focus.
export const motion = {
  dustSpeed: 1,
  ambientDrift: 1,
}
